version: '3.8'

services:
<<<<<<< HEAD
  # 1. База данных PostgreSQL
  db:
    image: postgres:15
    container_name: music_db
=======
  db:
    image: postgres:15
    container_name: music_db
    restart: always
>>>>>>> 18b90c4 (track date time for upload)
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: music_app
    ports:
<<<<<<< HEAD
      - "5432:5432" # Оставляем доступ снаружи для DBeaver/PgAdmin
    volumes:
      - ./pg_data:/var/lib/postgresql/data # Сохраняем данные на диске

  # 2. Хранилище MinIO
=======
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

>>>>>>> 18b90c4 (track date time for upload)
  minio:
    image: minio/minio:latest
    container_name: music_minio
    restart: always
    command: server /data --console-address ":9001"
<<<<<<< HEAD
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - ./minio_data:/data

  # 3. Твое приложение (Backend)
  backend:
    build: ./app # Строим из Dockerfile в текущей папке
    container_name: music_backend
    ports:
      - "8001:8000" # Пробрасываем порт: снаружи 8001 -> внутри 8000
    depends_on:
      - db
      - minio
    environment:
      # Настройки для подключения ВНУТРИ сети Docker
      # Обрати внимание: хосты называются 'db' и 'minio' (как имена сервисов выше)
      DATABASE_URL: postgresql://admin:admin@db:5432/music_app

      # Настройки S3
      S3_ENDPOINT_URL: http://minio:9000
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      S3_BUCKET_NAME: music-app-bucket
      S3_REGION_NAME: us-east-1

    # Команда запуска: сначала накатываем миграции, потом запускаем сервер
    command: >
      sh -c "alembic upgrade head &&
             uvicorn main:app --host 0.0.0.0 --port 8000"
=======
    environment:
      MINIO_ROOT_USER: "minioadmin"
      MINIO_ROOT_PASSWORD: "minioadmin"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  app:
    build:
      context: .             # Контекст сборки — корень проекта (где Dockerfile)
      dockerfile: Dockerfile
    container_name: music_backend
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - db
      - minio
    volumes:
      # ВАЖНО: Пробрасываем локальную папку ./app в папку /src внутри контейнера
      - ./app:/src
    environment:
      DATABASE_URL: postgresql://admin:admin@db:5432/music_app
      S3_ENDPOINT_URL: http://minio:9000
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      S3_BUCKET_NAME: music-bucket
      S3_REGION_NAME: us-east-1

volumes:
  postgres_data:
  minio_data:
>>>>>>> 18b90c4 (track date time for upload)
